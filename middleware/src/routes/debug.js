const express=require('express');const router=express.Router();const db=require('../db/postgres');router.get('/info',async(req,res)=>{const t=req.tenant.id;const v=await db.query('SELECT COUNT(*) as c FROM dash_vendas WHERE tenant_id=',[t]);const f=await db.query('SELECT COUNT(*) as c FROM dash_financeiro WHERE tenant_id=',[t]);res.json({vendas:v.rows[0].c, financeiro:f.rows[0].c});});module.exports=router;
